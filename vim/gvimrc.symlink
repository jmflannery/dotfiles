" hide toolbar and scroll bar 
set guioptions=Ace

" set window size
set lines=62 columns=180

" colorscheme
colorscheme codeschool

" GUI font
set guifont=Inconsolata:h16

" macVim: smooth fonts.
set antialias

" Use UTF-8 everywhere.
set encoding=utf-8
